# spectral query

An application for searching large audio databases based on spectral or transient features.
>
The user will be able to define search criteria and query an audio database with that intent, such as being able to find/sort sounds by spectral identifiers (eg. filter by bandwidth, etc). Queries for transients and percussive elements will also be supported, as well as common spectromorphological shapes.
